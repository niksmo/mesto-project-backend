/* eslint-disable no-unused-vars */
import { Types } from 'mongoose';

export const enum RelatedCardSchemaKeys {
  owner = 'owner',
  likes = 'likes',
}
export type IDataBaseObjectId = Types.ObjectId;
